exports.run = (client, message, args) => {
    if(message.author.id !== "627717611853185026") return message.channel.send(`**»** ${message.author}, you don't have permission to do that❌`);
               console.clear();
               client.destroy()
       return;
    }
